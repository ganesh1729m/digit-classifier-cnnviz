from django.contrib import admin
# from .models import MisclassifiedReport

# @admin.register(MisclassifiedReport)
# class MisclassifiedReportAdmin(admin.ModelAdmin):
#     list_display = ("id", "predicted_label", "true_label", "created_at")
#     list_filter = ("created_at",)
#     search_fields = ("predicted_label", "true_label")

# Register your models here.
